---
title: "Properties.Section.ModifyLibrary()"
description: "Unknown Description"
version_introduced: "5.0.1"
available_versions: "all"
ribbon: "Properties > Section > ModifyLibrary"
---

## Description

Unknown Description

## Syntax

```psj
Properties.Section.ModifyLibrary(strName="", crSection=None, iType=0, iLibType=0, dDimSize0=0, dDimSize1=0, dDimSize2=0, dDimSize3=0, dDimSize4=0, dDimSize5=0, dDimSize6=0, dDimSize7=0, dDimSize8=0, dDimSize9=0, dDimSize10=0, dDimSize11=0)
```

## Inputs

### `strName` @type(String) @default("")

- The name.

### `crSection` @type(Cursor) @default(None)

- The section.

### `iType` @type(Integer) @default(0)

- The type.

### `iLibType` @type(Integer) @default(0)

- The lib type.

### `dDimSize0` @type(Double) @default(0)

- The dim size0.

### `dDimSize1` @type(Double) @default(0)

- The dim size1.

### `dDimSize2` @type(Double) @default(0)

- The dim size2.

### `dDimSize3` @type(Double) @default(0)

- The dim size3.

### `dDimSize4` @type(Double) @default(0)

- The dim size4.

### `dDimSize5` @type(Double) @default(0)

- The dim size5.

### `dDimSize6` @type(Double) @default(0)

- The dim size6.

### `dDimSize7` @type(Double) @default(0)

- The dim size7.

### `dDimSize8` @type(Double) @default(0)

- The dim size8.

### `dDimSize9` @type(Double) @default(0)

- The dim size9.

### `dDimSize10` @type(Double) @default(0)

- The dim size10.

### `dDimSize11` @type(Double) @default(0)

- The dim size11.

## Return Code

A String of 1 if success, or 0 if fail.

## Sample Code

```psj
Properties.Section.ModifyLibrary(strName="", crSection=None, iType=0, iLibType=0, dDimSize0=0, dDimSize1=0, dDimSize2=0, dDimSize3=0, dDimSize4=0, dDimSize5=0, dDimSize6=0, dDimSize7=0, dDimSize8=0, dDimSize9=0, dDimSize10=0, dDimSize11=0)
```
