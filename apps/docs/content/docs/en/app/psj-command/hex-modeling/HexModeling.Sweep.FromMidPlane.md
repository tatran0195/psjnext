---
title: "HexModeling.Sweep.FromMidPlane()"
description: "Unknown Description"
version_introduced: "5.0.1"
available_versions: "all"
ribbon: "HexModeling > Sweep > FromMidPlane"
---

## Description

Unknown Description

## Syntax

```psj
HexModeling.Sweep.FromMidPlane(crlParts=[], bRef=True)
```

## Inputs

### `crlParts` @type(List\[Cursor]) @default(\[])

- The part.

### `bRef` @type(Boolean) @default(True)

- The reference.

## Return Code

A String of 1 if success, or 0 if fail.

## Sample Code

```psj
HexModeling.Sweep.FromMidPlane(crlParts=[], bRef=True)
```
