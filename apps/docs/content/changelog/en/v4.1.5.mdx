---
id: v4-1-5
slug: 'v4-1-5'
date: '2024-10-15'
version: 'v4.1.5'
title: 'Release Note V4.1.5'
summary: |
    This release mainly focuses on performance improvements along with few enhancements and bug
    fixes.
tags: ['Legacy', 'Release']
---

:::tip[Highlight updates]

This release mainly focuses on performance improvements along with few enhancements and bug fixes.

:::

## 🚀 New Feature

### 🛠 PSJ-Utility

#### 📝 API

API commands related to database query are added:

<details>
<summary>List of newly added commands</summary>

- `JPT.GetProgramVersion()` : Get Jupiter current release number.
- `JPT.GetContactType()` : Get Abaqus contact type.

</details>

## 🐛 Bug Fix

- After importing a Parasolid file (in foreground and background mode), if a part or
  _[DItem](/docs/data-type/psj-utility/pre-utility/built-in-types/DItem)_ with Japanese name in the assembly tree,
  Jupiter cannot read, put into `part.name` and print out. For example:

```psj
parts = JPT.GetAllParts()
for part in parts:
    print(part.name)
```

- Jupiter crashes when `PYTHONHOME` and `PYTHONPATH` are set by other software.
